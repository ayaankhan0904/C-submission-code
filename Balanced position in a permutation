#include <iostream>
#include <vector>
Using namespace std;
Int main() {
    Int N;
    Cin >> N;
    Vector<int> P(N);
    For (int I = 0; I < N; i++) {
        Cin >> P[i];
    }
    Int count = 0;
    For (int I = 0; I <= N; i++) { 
        If (P[i] = I + 1) {          
            Count++;
        }
    }
    Cout << cnt;   
    Return 0
}
